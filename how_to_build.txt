cd revm_wrapper
cargo clean
cargo build --release
cp target/release/libthrylos_revm.a ../lib/
cp target/release/libthrylos_revm.dylib ../lib/
cd ..
go test -run TestForkChoice_ReorgDepthLimit ./consensus/pos -v

If it doesn't work close and open the browser back up 

